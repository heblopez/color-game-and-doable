import { DateFormatedPipe } from './date-formated.pipe';

describe('DateFormatedPipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormatedPipe();
    expect(pipe).toBeTruthy();
  });
});
